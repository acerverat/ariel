# Parametros del flujo de trabajo ARIEL.
# Generado por scripts/generaParams.sh
# Pasar a Nextflow con: nextflow run main.nf -c config/nextflow.config -params-file config/params.yaml

# Rutas de entrada
runSampleSheet: "test_sample_sheet.tsv"
referenceDir:   "~/Pruebas_Reporte_LLAB/referencias/"
tpmPanel:       "~/Pruebas_Reporte_LLAB/datos/tpmPanel_fixed.tsv"

# Rutas de salida
resultsDir: "./resultados"
reportsDir: "./reportes"

# Hilos
threadsSTAR:    1
threadsRascall: 4

# Anotacion de variantes
snpeffGenome:   "GRCh38"
